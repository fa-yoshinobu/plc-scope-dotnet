<Window x:Class="PlcScope.App.ConnectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="接続設定"
        Width="760"
        Height="560"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource AppForegroundBrush}">
  <Window.Resources>
    <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
  </Window.Resources>
  <DockPanel Margin="16">
    <StackPanel DockPanel.Dock="Bottom"
                Orientation="Horizontal"
                HorizontalAlignment="Right"
                Margin="0,12,0,0">
      <Button Content="OK" Width="100" Margin="0,0,8,0" Click="OkButton_Click" Style="{StaticResource AccentButtonStyle}" />
      <Button Content="キャンセル" Width="100" IsCancel="True" />
    </StackPanel>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
      <StackPanel>
        <GroupBox Header="共通" Margin="0,0,0,12">
          <Grid Margin="8">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="160" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="プロトコル" VerticalAlignment="Center" Margin="0,0,8,8" />
            <ComboBox Grid.Row="0" Grid.Column="1"
                      Margin="0,0,0,8"
                      ItemsSource="{Binding Protocols}"
                      DisplayMemberPath="DisplayName"
                      SelectedItem="{Binding SelectedProtocol}" />

            <TextBlock Grid.Row="1" Grid.Column="0" Text="ホスト" VerticalAlignment="Center" Margin="0,0,8,8" />
            <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" Text="{Binding Host, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Grid.Row="2" Grid.Column="0" Text="ポート" VerticalAlignment="Center" Margin="0,0,8,8" />
            <TextBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,8" Text="{Binding Port}" />

            <TextBlock Grid.Row="3" Grid.Column="0" Text="タイムアウト (秒)" VerticalAlignment="Center" Margin="0,0,8,0" />
            <TextBox Grid.Row="3" Grid.Column="1" Width="120" Text="{Binding TimeoutSeconds}" />
          </Grid>
        </GroupBox>

        <GroupBox Header="通信" Margin="0,0,0,12">
          <StackPanel Orientation="Horizontal" Margin="8">
            <TextBlock Text="方式" VerticalAlignment="Center" Margin="0,0,8,0" />
            <ComboBox Width="160" ItemsSource="{Binding TransportModes}" SelectedItem="{Binding Transport}" />
          </StackPanel>
        </GroupBox>

        <GroupBox Header="SLMP" Visibility="{Binding IsSlmpSelected, Converter={StaticResource BoolToVisibilityConverter}}" Margin="0,0,0,12">
          <Grid Margin="8">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="180" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="PLC ファミリ" VerticalAlignment="Center" Margin="0,0,8,8" />
            <ComboBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" ItemsSource="{Binding SlmpFamilies}" SelectedItem="{Binding SlmpPlcFamilyName}" />

            <TextBlock Grid.Row="1" Grid.Column="0" Text="ネットワーク" VerticalAlignment="Center" Margin="0,0,8,8" />
            <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" Text="{Binding SlmpNetwork}" />

            <TextBlock Grid.Row="2" Grid.Column="0" Text="局番" VerticalAlignment="Center" Margin="0,0,8,8" />
            <TextBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,8" Text="{Binding SlmpStation}" />

            <TextBlock Grid.Row="3" Grid.Column="0" Text="モジュール I/O" VerticalAlignment="Center" Margin="0,0,8,8" />
            <TextBox Grid.Row="3" Grid.Column="1" Margin="0,0,0,8" Text="{Binding SlmpModuleIo}" />

            <TextBlock Grid.Row="4" Grid.Column="0" Text="監視タイマ" VerticalAlignment="Center" Margin="0,0,8,0" />
            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding SlmpMonitoringTimer}" />
          </Grid>
        </GroupBox>

        <GroupBox Header="Host Link" Visibility="{Binding IsHostLinkSelected, Converter={StaticResource BoolToVisibilityConverter}}" Margin="0,0,0,12">
          <Grid Margin="8">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="180" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="機種" VerticalAlignment="Center" Margin="0,0,8,8" />
            <ComboBox Grid.Row="0" Grid.Column="1"
                      Margin="0,0,0,8"
                      ItemsSource="{Binding HostLinkModels}"
                      SelectedItem="{Binding HostLinkPlcModelName}" />
            <TextBlock Grid.Row="1" Grid.Column="0" Text="デバイスモード" VerticalAlignment="Center" Margin="0,0,8,0" />
            <ComboBox Grid.Row="1" Grid.Column="1"
                      ItemsSource="{Binding KeyenceDeviceModes}"
                      DisplayMemberPath="Label"
                      SelectedItem="{Binding SelectedKeyenceDeviceMode}" />
          </Grid>
        </GroupBox>

        <GroupBox Header="TOYOPUC" Visibility="{Binding IsToyopucSelected, Converter={StaticResource BoolToVisibilityConverter}}">
          <Grid Margin="8">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="180" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="機種" VerticalAlignment="Center" Margin="0,0,8,8" />
            <ComboBox Grid.Row="0" Grid.Column="1"
                      Margin="0,0,0,8"
                      ItemsSource="{Binding ToyopucDeviceProfiles}"
                      SelectedItem="{Binding ToyopucDeviceProfile}" />
            <TextBlock Grid.Row="1" Grid.Column="0" Text="中継ホップ" VerticalAlignment="Center" Margin="0,0,8,8" />
            <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" Text="{Binding ToyopucRelayHops}" />
            <TextBlock Grid.Row="2" Grid.Column="0" Text="ローカルポート" VerticalAlignment="Center" Margin="0,0,8,8" />
            <TextBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,8" Text="{Binding ToyopucLocalPort}" />
            <TextBlock Grid.Row="3" Grid.Column="0" Text="リトライ / 遅延 ms" VerticalAlignment="Center" Margin="0,0,8,0" />
            <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal">
              <TextBox Width="120" Margin="0,0,8,0" Text="{Binding ToyopucRetries}" />
              <TextBox Width="120" Text="{Binding ToyopucRetryDelayMs}" />
            </StackPanel>
          </Grid>
        </GroupBox>
      </StackPanel>
    </ScrollViewer>
  </DockPanel>
</Window>
