<Window x:Class="PlcScope.App.Windows.DeviceRangeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Device ranges"
        Width="980"
        Height="540"
        WindowStartupLocation="CenterOwner">
  <DockPanel Margin="12">
    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
      <TextBlock AutomationProperties.AutomationId="DeviceRangeModelText" Text="{Binding ModelText}" Margin="0,0,18,0" FontWeight="SemiBold" />
      <TextBlock AutomationProperties.AutomationId="DeviceRangeFamilyText" Text="{Binding FamilyText}" Foreground="{DynamicResource AppForegroundMutedBrush}" />
    </StackPanel>
    <DataGrid AutomationProperties.AutomationId="DeviceRangeDataGrid"
              ItemsSource="{Binding Entries}"
              AutoGenerateColumns="False"
              IsReadOnly="True"
              CanUserAddRows="False">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Device" Binding="{Binding Device}" Width="90" />
        <DataGridTextColumn Header="Type" Binding="{Binding Category}" Width="120" />
        <DataGridCheckBoxColumn Header="Bit" Binding="{Binding IsBitDevice}" Width="60" />
        <DataGridCheckBoxColumn Header="Supported" Binding="{Binding Supported}" Width="80" />
        <DataGridTextColumn Header="Points" Binding="{Binding PointCount}" Width="90" />
        <DataGridTextColumn Header="Lower" Binding="{Binding LowerBound}" Width="80" />
        <DataGridTextColumn Header="Upper" Binding="{Binding UpperBound}" Width="90" />
        <DataGridTextColumn Header="Range" Binding="{Binding AddressRange}" Width="170" />
        <DataGridTextColumn Header="Format" Binding="{Binding Notation}" Width="90" />
        <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="150" />
        <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="*" />
      </DataGrid.Columns>
    </DataGrid>
  </DockPanel>
</Window>
