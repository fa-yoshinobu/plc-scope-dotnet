<Window x:Class="PlcScope.App.Windows.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Password"
        Width="360"
        Height="150"
        WindowStartupLocation="CenterOwner">
  <DockPanel Margin="12">
    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
      <Button Content="OK" Width="90" Margin="0,0,8,0" Click="OkButton_Click" Style="{StaticResource AccentButtonStyle}" />
      <Button Content="Cancel" Width="90" IsCancel="True" />
    </StackPanel>
    <StackPanel>
      <TextBlock Text="Enter the remote password only when required." Margin="0,0,0,8" />
      <PasswordBox x:Name="PasswordBox" />
    </StackPanel>
  </DockPanel>
</Window>
