<Window x:Class="PlcScope.App.Windows.AboutWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About PLC Scope"
        Width="700"
        Height="560"
        MinWidth="620"
        MinHeight="500"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource AppForegroundBrush}">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="8" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Border BorderBrush="{DynamicResource AppBorderBrush}" BorderThickness="1" Padding="10">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="56" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Image Grid.Column="0"
               Width="40"
               Height="40"
               HorizontalAlignment="Left"
               VerticalAlignment="Top"
               Source="/icon.ico" />
        <StackPanel Grid.Column="1">
          <TextBlock Text="PLC Scope" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource AppForegroundStrongBrush}" />
          <TextBlock x:Name="VersionTextBlock" Margin="0,4,0,0" />
          <TextBlock Margin="0,2,0,0" Text="Company: FA Labo" />
          <TextBlock Margin="0,2,0,0" Text="Copyright: FA Labo(fa_yoshinobu)" />
          <TextBlock Margin="0,6,0,0">
            <Run Text="GitHub: " />
            <Hyperlink NavigateUri="https://github.com/fa-yoshinobu/plc-scope-dotnet"
                       RequestNavigate="Hyperlink_RequestNavigate">
              https://github.com/fa-yoshinobu/plc-scope-dotnet
            </Hyperlink>
          </TextBlock>
        </StackPanel>
      </Grid>
    </Border>

    <TabControl Grid.Row="2">
      <TabItem Header="Libraries">
        <Grid Margin="8">
          <ListView x:Name="LibrariesListView"
                    Focusable="False"
                    SelectionMode="Single">
            <ListView.ItemContainerStyle>
              <Style TargetType="{x:Type ListViewItem}">
                <Setter Property="Focusable" Value="False" />
                <Setter Property="IsHitTestVisible" Value="False" />
                <Setter Property="Foreground" Value="{DynamicResource AppForegroundBrush}" />
              </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
              <GridView>
                <GridViewColumn Header="Library" Width="220" DisplayMemberBinding="{Binding Name}" />
                <GridViewColumn Header="Version" Width="90" DisplayMemberBinding="{Binding Version}" />
                <GridViewColumn Header="License" Width="120" DisplayMemberBinding="{Binding License}" />
                <GridViewColumn Header="Notes" Width="230" DisplayMemberBinding="{Binding Notes}" />
              </GridView>
            </ListView.View>
          </ListView>
        </Grid>
      </TabItem>

      <TabItem Header="Open Source Licenses">
        <Grid Margin="8">
          <TextBox x:Name="LicenseTextBox"
                   IsReadOnly="True"
                   AcceptsReturn="True"
                   TextWrapping="Wrap"
                   VerticalScrollBarVisibility="Auto"
                   HorizontalScrollBarVisibility="Disabled" />
        </Grid>
      </TabItem>
    </TabControl>

    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Width="90" Height="30" Content="Close" Click="Close_Click" />
    </StackPanel>
  </Grid>
</Window>
