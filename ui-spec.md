# PLC スコープ — 画面仕様書

> 対象: Windows デスクトップアプリ (.NET 9 / WPF)  
> ライブラリ: PlcComm.Slmp / PlcComm.KvHostLink / PlcComm.Toyopuc

---

## 1. 全体レイアウト

```
┌─────────────────────────────────────────────────────────────────┐
│ [メニューバー] ファイル | 接続 | 表示 | 書込 | ツール | ヘルプ  │
├─────────────────────────────────────────────────────────────────┤
│ [接続バー]                                                       │
├─────────────────────────────────────────────────────────────────┤
│ [ブロック設定バー]                                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│ [ブロックグリッド] (スクロール可)                                │
│                                                                  │
├─────────────────────────────────────────────────────────────────┤
│ [書込パネル]                                                     │
├─────────────────────────────────────────────────────────────────┤
│ [ステータスバー]                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. メニューバー

| メニュー | 項目 |
|----------|------|
| ファイル | 新規プロジェクト / プロジェクトを開く / 保存 / 名前を付けて保存 / 終了 |
| 接続 | 接続設定… / 接続 / 切断 / プリセット管理… |
| 表示 | Word モード / DWord モード / Bit 展開モード / Float32 モード / 10進 / 16進 / 2進 |
| 書込 | 書込ロック ON/OFF / 選択アドレスへ書込… / 一括書込(範囲)… / CPU RUN / CPU STOP |
| ツール | アドレスジャンプ… / エラー履歴… / 通信ログ… / 設定… |
| ヘルプ | バージョン情報 |

---

## 3. 接続バー

常時表示。左から右に以下を配置。

| 要素 | 詳細 |
|------|------|
| [接続設定] ボタン | クリックで接続設定ダイアログを開く |
| [接続] / [切断] トグルボタン | 接続中は「切断」に変化 |
| ステータスインジケーター | ●(緑)接続中 / ●(グレー)切断中 / ●(赤)エラー + テキスト |
| メーカー選択ドロップダウン | 三菱 MELSEC (SLMP) / KEYENCE KV (HostLink) / JTEKT TOYOPUC |

### 接続設定ダイアログ

| 項目 | 型 | デフォルト |
|------|----|-----------|
| プロトコル | ラジオ (SLMP / HostLink / ComputerLink) | SLMP |
| ホスト | テキスト (IP) | 192.168.1.10 |
| ポート | 数値 | 1025 (SLMP/CL) / 8501 (HL) |
| PLC ファミリ | ドロップダウン ※SLMP時のみ | IqR |
| タイムアウト | 数値 (秒) | 3 |
| 転送モード | ラジオ (TCP / UDP) | TCP |

PLC ファミリ選択肢 (SLMP): IqR / IqF / IqL / QnU / QnUDV / MxR / MxF

---

## 4. ブロック設定バー

| 要素 | 説明 |
|------|------|
| デバイス種別 ▼ | メーカーに応じてリスト変化（下記参照） |
| 先頭アドレス | テキスト入力。記号省略可 (例: `100` → `D100`) |
| 点数 / ワード数 | スピナー。Word/Bit=点数、DWord/Float=ワード数 (÷2して表示) |
| 形式 | Word / DWord / Bit / Float32 のラジオボタン |
| Bit表示 ※ビットデバイス選択時のみ | **16点まとめ** / **1点1行** のラジオボタン |
| 表示進数 ▼ | 10進 / 16進 / 2進 |
| [▶ 読込] ボタン | 1回読込実行 |
| [■ 停止] ボタン | 自動更新停止 (更新中のみ表示) |
| 自動更新 チェック + 間隔 (ms) | チェックONで DispatcherTimer 駆動の繰り返し読込 |

### デバイス種別リスト

**三菱 MELSEC (SLMP)**

| 区分 | デバイス |
|------|---------|
| ビット | X, Y, M, SM, B, SB, F, L, V, TS, TC, STS, STC, LTS, LTC, CS, CC |
| ワード | D, SD, W, R, ZR, TN, LTN, STN, CN, LCN, SW, Z, LZ |

**KEYENCE KV (HostLink)**

| 区分 | デバイス |
|------|---------|
| ビット | X, Y, R, MR, LR, CR, M, L, VB |
| ワード | DM, EM, FM, W, ZF, TM, Z, T, C, CM, AT, VM |

**JTEKT TOYOPUC (ComputerLink)**

| 区分 | デバイス |
|------|---------|
| ビット | P1-M, P1-X, P1-Y, P1-T, P1-C |
| ワード | P1-D, P1-S, P1-N, P1-R, P1-ES, P1-EN, P1-FR |

---

## 5. ブロックグリッド

### 5-1. Word モード (ワードデバイス選択時)

列構成:

| 列 | 内容 | 幅 | 備考 |
|----|------|----|------|
| アドレス | D00100 | 110px | 固定 |
| 値 | 10進 or 16進の TextBox | 100px | Enter=書込確定、Esc=キャンセル |
| Hex | 16進4桁 | 60px | 値と連動、読取専用 |
| ビット (16個) | 丸ボタン ×16 | 可変 | 各ビットをクリックで即時反転・書込 |
| コメント | 文字列 | 可変 | 読取専用 |

**ビット16個ボタン仕様:**
- 左から bit15, bit14, … bit0 の順で表示
- 4個ずつグループ化して間隔を空ける (b15~12 | b11~8 | b7~4 | b3~0)
- ON (1) = 緑の丸 / OFF (0) = グレーの丸
- クリック → そのビットを反転し、ワード値を書込
- 値 TextBox を編集して Enter → 全16ボタンも連動更新

### 5-2. Word モード (ビットデバイス選択時) — 16点まとめ

| 列 | 内容 |
|----|------|
| アドレス | M00000 +15 (ブロック先頭+範囲) |
| 16点表示 | — |
| ビット (16個) | 各ドット = 独立した1ビットアドレス (M00000~M00015) |
| コメント | 空 |

- 1行 = 16連続ビットアドレスをまとめて表示
- 各丸ボタンの tooltip にアドレスを表示 (M00003 等)
- クリック → そのアドレスのビット1点を書込

### 5-3. Word モード (ビットデバイス選択時) — 1点1行

| 列 | 内容 |
|----|------|
| アドレス | M00050 |
| ON/OFF | [● ON] / [○ OFF] トグルボタン |
| 値 | 1 / 0 |

- クリック → 即時書込

### 5-4. DWord モード

| 列 | 内容 |
|----|------|
| アドレス | D00100 (2W) |
| 値 | 32bit 数値 TextBox |
| Hex | 8桁16進 |
| ビット32個 | 4個グループ×8 で表示 (読取専用) |
| コメント | — |

- 1行 = 2ワード消費 (stride=2)
- アドレス列に `(2W)` サフィックス

### 5-5. Float32 モード

| 列 | 内容 |
|----|------|
| アドレス | D00100 (F) |
| 値 | 浮動小数点 TextBox (例: 3.1416) |
| Hex | IEEE754 8桁 |
| コメント | — |

### 5-6. Bit 展開モード

ワードデバイスを1ワード→16ビット行に展開表示。

```
▼ D00100  = 1234  /  0x04D2  [0000 0100 1101 0010]   ← グループヘッダー行
    D00100.0   b0   [○ OFF]
    D00100.1   b1   [● ON ]
    ...
    D00100.15  b15  [○ OFF]
▼ D00101  = 0     /  0x0000  [0000 0000 0000 0000]
    D00101.0   b0   [○ OFF]
    ...
```

- グループヘッダー行: ワードアドレス + 値 + Hex + 16ビット一覧 (読取専用)
- ビット行: アドレス `.N` 形式、bit番号、ON/OFFトグルボタン
- トグルボタンクリック → `WriteBitInWordAsync(addr, bitIdx, value)` または `WriteBitsAsync`

### 5-7. 共通インタラクション

| 操作 | 動作 |
|------|------|
| 行クリック | 書込パネルのアドレスに自動セット |
| 値 Enter | 書込実行、行が一時黄色フラッシュ |
| 値 Esc | 編集キャンセル、元の値に戻る |
| ビット丸ボタンクリック | 即時書込、行が一時フラッシュ |
| 書込ロック ON 時 | すべての書込操作をグレーアウト・無効化 |

---

## 6. 書込パネル (下部固定)

| 要素 | 詳細 |
|------|------|
| 書込先アドレス | テキスト (行クリックで自動セット) |
| データ型 ▼ | Bit / Int16 / UInt16 / Int32 / UInt32 / Float32 |
| 値 | テキスト入力 |
| 進数 | ラジオ (10進 / 16進 / 2進) |
| [書込実行] | 確認ダイアログ後に書込 (確認OFF設定可) |
| [書込ロック] トグル | ON時はすべての書込を禁止、ボタン赤表示 |

---

## 7. ステータスバー

左から: 接続状態 | 最終読込時刻 | 応答時間 (ms) | (右端) エラーメッセージ

---

## 8. 状態・ふるまい

### 接続状態遷移

```
切断中 ──[接続ボタン]──▶ 接続中
  ▲                          │
  └──[切断/エラー/タイムアウト]┘
```

- 接続中のみ読込・書込・自動更新が有効
- 切断時は自動更新を自動停止

### 自動更新

- `DispatcherTimer` を使用 (UI スレッド安全)
- 読込は `ReadWordsChunkedAsync` / `ReadBitsAsync` を非同期で呼び出し
- 応答時間を計測してステータスバーに表示

### 書込フロー (Word)

1. ユーザーが値入力 → Enter
2. `WriteWordsSingleRequestAsync(addr, [value])` を呼び出し
3. 成功 → 行フラッシュ + ステータス更新
4. 失敗 → エラーダイアログ + エラー履歴に追記

### 書込フロー (Bit 丸ボタン)

- ワードデバイスのビット操作: `WriteBitInWordAsync(wordAddr, bitIndex, value)`
- ビットデバイス1点操作: `WriteBitsAsync(addr, [value])`

---

## 9. カラー定義

| 用途 | 色 |
|------|----|
| 背景 | `#1E1E1E` |
| パネル背景 | `#252526` / `#2D2D2D` |
| 選択行 | `#0E3A5C` |
| ビット ON | `#27AE60` (緑) |
| ビット OFF | `#2A2A2A` (グレー) |
| 書込フラッシュ | `#3A3A00` → フェードアウト |
| アクセントブルー | `#0E639C` |
| エラー | `#E74C3C` |
| アドレス文字 | `#9CDCFE` |
| 値文字 | `#DCDCAA` |
| Hex文字 | `#CE9178` |
| コメント文字 | `#6A9955` |

---

## 10. WPF 実装メモ

### DataGrid セル型の切替

`DataTemplateSelector` でデバイス種別・表示形式に応じてセルテンプレートを切替:

- ワードデバイス行 → `TextBox` + `BitDotsPanel` (16ボタン)
- ビットデバイス16点まとめ行 → `BitDotsPanel` のみ
- ビットデバイス1点行 → `ToggleButton`
- Bit展開グループヘッダー → `TextBlock` (読取専用)

### ビット丸ボタン

`ItemsControl` + `UniformGrid(Columns=4)` × 4グループ で実装:

```xml
<ItemsControl ItemsSource="{Binding BitGroups}">
  <ItemsControl.ItemsPanel>
    <ItemsPanelTemplate>
      <StackPanel Orientation="Horizontal"/>
    </ItemsPanelTemplate>
  </ItemsControl.ItemsPanel>
  <ItemsControl.ItemTemplate>
    <DataTemplate> <!-- 4個グループ -->
      <UniformGrid Columns="4">
        <ToggleButton Style="{StaticResource BitDotStyle}" .../>
        ...
      </UniformGrid>
    </DataTemplate>
  </ItemsControl.ItemTemplate>
</ItemsControl>
```

### 非同期書込の UI ロック

書込中は `IsEnabled=false` でグリッドをロックせず、`CancellationToken` 付きの書込キューで順序を保証。

---

## 11. 関連ファイル

| ファイル | 内容 |
|----------|------|
| `ui-mockup.html` | インタラクティブ HTML モックアップ |
| `ui-spec.md` | 本仕様書 |
